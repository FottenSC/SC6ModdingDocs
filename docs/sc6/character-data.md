# Character Data

!!! info "Stub"
    Document character DataTables, move lists, and parameter assets here.

## Sections to fill in

- DataTable paths (e.g. `/Game/Chara/...`)
- Move data row schema
- Where parameters are consumed at runtime (which UFunctions read them)
- Known safe edits vs. edits that trip anti-tamper
